{
  "_comment": "Drop this into your MCP client config (e.g. Claude Desktop's claude_desktop_config.json, or a Claude Code .mcp.json). Build the binary first: dart compile exe packages/mcp/bin/gpsphototag_mcp.dart -o gpsphototag_mcp, then point 'command' at it.",
  "mcpServers": {
    "gpsphototag": {
      "command": "/absolute/path/to/gpsphototag_mcp"
    }
  }
}
